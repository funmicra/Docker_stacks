---
version: "2.1"
services:
  code-server:
    image: lscr.io/linuxserver/code-server:latest
    container_name: code-server
    environment:
      - PUID=1000
      - PGID=1000
      - TZ=Europe/Athens
      - PASSWORD=H84zzoMc #optional
#      - HASHED_PASSWORD= #optional
      - SUDO_PASSWORD=H84zzoMc #optional
#      - SUDO_PASSWORD_HASH= #optional
#      - PROXY_DOMAIN=code-server.my.domain #optional
#      - DEFAULT_WORKSPACE=/config/workspace #optional
    volumes:
      - /home/funmicra/Appdata/code-server:/config
    ports:
      - 2443:8443
    restart: unless-stopped


networks:
  default:
    external:
      name: Apps-Network
